# Authentication fixtures

Fixtures in this directory must be synthetic or irreversibly redacted. They must not be copied from a real Codex account. The current project intentionally does not include a real Codex auth schema because the local format and refresh contract have not been verified as a public stable interface.
